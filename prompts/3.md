Atua como Senior Fullstack Engineer e UI/UX Designer. Vamos finalizar o backend integrando uma API robusta e um dashboard de monitorização MODERNO e bonito.

Lógica:
No ficheiro reports/views.py, cria uma função dashboard(request):
Deve permitir filtrar por device_id (User Agent), start_date e end_date.
Recupera todos os AirReport da base de dados (PostgreSQL), ordenados pelo mais recente.
Extrai uma lista única de device_id para popular um filtro dropdown.

No ficheiro reports/api.py, garante que o Django Ninja tem os seguintes endpoints:
POST /ping: Recebe coordenadas e AQI, guarda no modelo.
GET /history: Retorna os dados em JSON (usado pela App e para debugging).

Interface do Dashboard
Cria o ficheiro reports/templates/reports/dashboard.html.
Estética: Dark Mode 'Command Center' usando Tailwind CSS. Usa uma paleta de slate-950 para o fundo e emerald-500 para os destaques. Aplica efeitos de Glassmorphism nos cartões.
Header: Estatísticas rápidas (Total de Pings, Última Localização Ativa, Média de AQI).
Filtros: Uma barra lateral ou superior elegante com um dropdown para IDs de dispositivos e seletores de data.
O Mapa (Leaflet.js): Insere um mapa ocupando metade do ecrã.
Usa o tile layer Dark: https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png.
Plota todos os pings filtrados como marcadores. Ao clicar, mostra um popup com: ID do Dispositivo, AQI e Timestamp formatado.
Tabela de Dados: Uma lista formatada por baixo do mapa com scroll interno, com o histórico detalhado.

Configuração de URLs:
Atualiza o core/urls.py para que:
O caminho vazio path('', ...) carregue a view do dashboard.
O caminho path('api/v1/', api.urls) carregue a API do Ninja.

Garante que o código é limpo, utiliza classes utilitárias do Tailwind e que o mapa carrega via CDN para não precisar de configurações extra de build no backend.