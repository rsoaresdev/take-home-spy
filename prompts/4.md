Atua como Senior Fullstack Engineer. Vamos finalizar o projeto com as seguintes correções críticas e novas funcionalidades.

Backend:
Fix do Mapa: no views.py, transforma o QuerySet em JSON real usando list(reports.values('id', 'device_id', 'latitude', 'longitude', 'aqi_value', 'timestamp')) antes de passar para o template. O erro atual é passar um objeto QuerySet que o JavaScript não consegue ler.
Tradução: Traduz toda a interface do Dashboard para Português de Portugal (ex: 'Histórico de Telemetria', 'Agente', 'Data/Hora').
Template: Garante que o script do Leaflet faz um loop correto sobre este JSON para colocar os pins no mapa.

App: UI Refactor (Pull-to-Refresh & Uplink Switch)
Pull-to-Refresh: Adiciona um ScrollView com RefreshControl. Ao puxar para baixo, a app deve:
Forçar uma nova leitura do GPS.
Enviar um ping manual imediato para o backend.

Uplink Status: Ao ativar o uplink (ao carregar 5 vezes no número de AQI) deve aparecer confirmação visual em toaster.
Toaster: Usa react-native-root-toast ou similar para mostrar mensagens: 'Uplink Ativado 📡' e 'Uplink Desativado 🔐'.

App: Histórico e Mapa Integrado
Cria um novo componente/ecrã (ou uma aba expansível) que mostre um mapa nativo (react-native-maps).
Este mapa deve carregar o histórico do utilizador (via GET /history) e mostrar marcadores com: Data, Hora e Valor AQI formatados.

Fix: Background Task
O background-air-sync não está a disparar. Provavelmente por falta de registo no ponto de entrada ou throttling do iOS.
Garante que TaskManager.defineTask é chamado no index.js ou no topo do App.js (fora do componente).
Adiciona console.log dentro da tarefa para vermos no terminal cada vez que ela dispara.
Configura o Location.startLocationUpdatesAsync com deferredUpdatesInterval: 60000 e accuracy: Location.Accuracy.Balanced.

Utiliza apenas NativeWind para estilos e garante que a estética é Dark Mode Industrial